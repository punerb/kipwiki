<%= form_for(@project_objective) do |f| %>
  <% if @project_objective.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@project_objective.errors.count, "error") %> prohibited this project_objective from being saved:</h2>

      <ul>
      <% @project_objective.errors.full_messages.each do |msg| %>
        <li><%= msg %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :name %><br />
    <%= f.text_field :name %>
  </div>
  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
