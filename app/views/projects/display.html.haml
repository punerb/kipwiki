#content
  / div right start
  .right
    = render "left_bar"
    %b 
      = link_to 'Back to project details', show_project_path(@project.city, @project.slug)
  .left
    = render :partial => "projects/display/#{@selection}"
  / div left End
  .clr
/ content end
.clr
/ footer Start
#footer
/ footer end
.clr
-#:javascript
-#  -#var editor = new punymce.Editor({id : 'helo'});

