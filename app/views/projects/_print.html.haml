- unless print.new_record?
  .photo{:id => "print#{print.id}"}
    = image_tag(print.attachment.url(:thumb))
    - if current_user_is_owner(print.project) 
      = link_to image_tag('delete.png'), delete_attachment_path(print), :remote => true, :title => 'Delete Photo'