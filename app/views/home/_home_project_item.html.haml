%li{:class => margin_class}
  .heading
    = link_to project.title.truncate(40), show_project_path(project.city.parameterize, project.slug)
  .imageWrapper
    %img{:alt => "", :height => "193", :src => "images/testPhoto.jpg", :width => "258"}/
  .voteViewMurge
    .viewWrapper
      = project.view_count
      Views
    .clr
  .clr
  .textWrapper
    .heading
      Address
    .clr
    = project.address
  .textWrapper
    .heading
      Summary
    .clr
    = project.description.truncate(160).gsub(/<.*>/, "")
    = link_to "more", show_project_path(project.city.parameterize, project.slug)